<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Piktosaur.Views.SlideshowWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Piktosaur.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Slideshow">

    <Grid x:Name="RootGrid"
          Background="Black"
          KeyboardAcceleratorPlacementMode="Hidden"
          PointerMoved="OnPointerMoved">
        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator Key="Escape" Invoked="OnEscapePressed" />
            <KeyboardAccelerator Key="Left" Invoked="OnLeftPressed" />
            <KeyboardAccelerator Key="Right" Invoked="OnRightPressed" />
            <KeyboardAccelerator Key="Space" Invoked="OnSpacePressed" />
        </Grid.KeyboardAccelerators>
        <Image x:Name="SlideshowImage"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center" />

        <local:SlideshowControls x:Name="Controls"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Bottom"
                                 Margin="0,0,0,48" />
    </Grid>
</Window>
